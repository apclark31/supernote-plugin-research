# SuperTask: Features & Bugs Tracker

> Active features, tasks, and bugs for the SuperTask plugin. Each item has a unique ID, type label, status, and optional link to a design doc.
>
> **Related docs:**
> - Design docs: `docs/design-*.md` -- deep dives on specific features
> - Changelog: `docs/changelog.md` -- completed/resolved items move here
> - Session state: `PROGRESS.md` -- current session handoff notes
>
> When an item is completed or resolved, move it to `changelog.md` with the date and a one-line summary of the outcome.

## Features

| ID | Status | Title | Design doc | Notes |
|----|--------|-------|------------|-------|
| F-001 | Open | Inline task markers (T/ST) | `design-task-markers.md` | Embed marker into text content instead of separate T badge element. Solves grouping problem (badge doesn't move with content). Supports future subtask markers (ST). |
| F-002 | Done | Text editability on convert | -- | Resolved by SDK behavior: text is editable via pen long-press or finger double-tap (Supernote built-in gestures). |
| F-003 | Backlog | Subtask support | -- | `parent_id` in Todoist API. Subtask list in detail view. ST marker on note. |
| F-004 | Backlog | Task dashboard / master note | -- | Create a note with all captured tasks as linked entries. APIs confirmed ready. |
| F-005 | Backlog | Doc capture (PDF) | -- | PDF text selection, same flow as lasso handwriting. |
| F-006 | Backlog | Offline mode | `design-offline-mode.md` | Queue tasks locally, sync to Todoist when wifi available. |
| F-007 | Done | Config persistence via RNFS | `design-settings.md`, `design-architecture.md` | JSON config file in MyStyle/SuperTask/ read/written via react-native-fs. Works from any screen (no note context needed). Confirmed on-device. |
| F-008 | Done | Token obfuscation + auto-generation | `design-architecture.md` | XOR+base64 obfuscation replaces crypto-js (which broke on Hermes). Template config auto-generated on first launch. Plain text tokens auto-obfuscated on load. Info popup guides USB/BT/keyboard entry. |
| F-009 | Done | Motion listener gesture capture | `../../docs/gesture-research.md` | Long-press finger detection on supertask:// links. Pre-scan on DOWN, mixed-input rejection (pen, multi-pointer). Confirmed on-device. |
| F-010 | Open | Background processing with showPluginView | -- | Dismiss UI during API calls, reopen with results via `showPluginView()`. New in sn-plugin-lib 0.1.43. |
| F-011 | Done | View Note from TaskDetail | -- | Button in "Captured from" section. Same note: closes plugin with page hint. Different note: tries openFilePath (opens file manager, not editor), falls back to showing path. |
| F-012 | Done | Long-press gesture to open task from note | `../../docs/gesture-research.md` | Gesture detector in `gestureDetector.js`, registered at init (index.js). Pre-scans links on finger DOWN, navigates via `global.__superTaskNavigate` (re-show) or `getInitialScreen` (first mount). Single task API + parallel fetch. |
| F-013 | Done | Cross-note navigation | -- | Temp link at top-center of current page with task name. Auto-cleanup via `deleteElements` on next plugin open. Full path stored in Todoist description + task registry. |
| F-014 | Done (config-gated since session 34/B-028) | Three-finger double tap to launch task home | -- | **Session 34: now opt-in (`threeFingerTapEnabled`, default OFF), checkbox in Settings > Handwriting.** The gesture works ANYWHERE on canvas by design (no geometric constraint) -- which B-028 showed lets palm clusters mimic it. Only long press (geometrically targeted at link bounds) remains always-on. Original notes:  Replaced bezel swipe (unreliable due to digitizer edge issues + `_enabled` flag leak). Three-finger double tap anywhere on canvas, twice within 800ms, opens task home with user's default tab. No edge zone, no displacement calculation. Confirmed on A5X. |
| F-015 | Done | Quick lasso-add gesture (finger) | `design-gesture-guards.md` | Hold 400ms then drag to select and open QuickAdd. Finger only (pen mode removed). Existing-link gate now runs post-classification in `handleLassoAdd` (session 34; pre-scan removed per B-021). Config: off/finger/pen-lasso -- **default changed to 'off' in session 34** (hold-then-drag resembles a paused scroll; false activations mid-note-taking made the device feel unreliable). 'off' now disables only the quick-add gesture; long press + three-finger tap stay active (matches the Settings UI hint, which previously overpromised). |
| F-016 | Done | Pen lasso + finger hold to quick-add | -- | Hold finger on screen, draw native lasso with pen (visible selection), lift finger. Plugin intercepts via `getLassoRect()` check on finger UP -- if native lasso is active, opens QuickAdd. Error 904 (no lasso, just writing) is silently ignored. Config toggle: "Pen lasso" in Settings > Quick Add Gesture. Confirmed on-device. |
| F-017 | Done | Native intent navigation (replace temp links) | `design-native-intents.md` | Direct note/page navigation via Android Intent to `NoteInsidePagesActivity`. Phases 1-2 complete (NoteOpenerModule rewrite, callsite replacement). `tempLinkNav.js` deleted (session 32). Phase 3 (RNFS removal) tracked as F-018. Phase 4 (dashboard note) tracked as F-004. |
| F-018 | Open | Native writeFile (potential RNFS replacement) | `design-native-intents.md` | Add `writeFile(path, content)` to NoteOpenerModule via `java.io.FileWriter`. Could replace `react-native-fs` dependency (~2MB build savings). Requires auditing all RNFS usage first. Lower priority than F-017. |
| F-019 | Backlog | Quick page creation with date headers | -- | One-tap "add new page for today" on a note (e.g., "Weekly Team Meetings"): inserts a new page with today's date as a header, auto-populates TOC. Crossover with intent navigation (jump to the new page) and `createNote`/`insertNotePage`/`insertText` APIs. Could extend to templates (meeting notes, daily log). Inspired by dashboard plugin's note/page navigation capabilities. |
| F-020 | Backlog | Floating bubble launcher | -- | Three-finger double tap toggles a floating bubble (native Android overlay via `WindowManager`). Tapping the bubble launches SuperTask. Draggable, persists across pages. Settings to choose behavior: direct launch (current) vs bubble toggle. Inspired by AgP42/supernote-dashboard's floating UI. Lower priority -- direct three-finger double tap works well. |
| F-026 | Implemented (awaiting on-device test) | Note-jump button in task rows (On Device + This Note) | `design-home-v2.md` | Alex request: bring TaskDetail's View Note into the list. TaskRow gains an optional right-aligned bordered 'Note >' button (third tap target, distinct from checkbox and row-press). On Device rows: jumps via stored notePath, falling back to a same-directory guess for legacy registry entries (mirrors TaskDetail). This Note rows: shown ONLY for tasks on a DIFFERENT page than the current one (jumping to where you already are is noise) -- this is the 'know where you jump in a long note' payoff. Uses openNote() intent (0-based registry page -> 1-based intent; plugin view closes itself). |
| F-025 | Implemented (awaiting on-device test) | Arm-then-confirm completion + Done tab with reopen | `design-home-v2.md` | Alex flagged that the row checkbox completed tasks in one tap, sight-unseen (behavior predates the redesign -- the old glyph did the same; the redesign just made it visible). Fix pairs deliberateness with recoverability: (1) **arm-then-confirm** -- first tap fills the box (fixed geometry) and shows an inverted 'Tap again to complete' chip; second tap within 3s completes; auto-disarms otherwise. (2) **Done tab** (fifth tab): completed tasks from the last 30 days via v1 `/tasks/completed/by_completion_date` (lazy-fetched on first visit, not part of the main cache), bucketed Today/Yesterday/This Week/Earlier, chips incl. 'Done <date>' + project; tapping the filled box REOPENS the task (`/tasks/{id}/reopen`) and pulls it back into active lists -- completion is now fully recoverable. Footer Refresh refetches the done list when on that tab. VERIFY ON-DEVICE: the completed-tasks endpoint path is per v1 unified API docs -- if it 404s, check the dev-server log and adjust the path. Known nuance: opening a done task's detail shows metadata but Complete would fail (it is already done) -- acceptable for v1. |
| F-024 | Implemented (awaiting on-device test) | TaskHome redesign v2 (chips, drawn controls, This Note band) | `design-home-v2.md` | Extends the settings design language app-wide. Chip component (bordered metadata tags -- the ONE idiom for due/priority/project/page/sync-state and every count; overdue inverts); TaskRow uses the shared drawn Check box (glyph checkbox retired); TabBar -> inverted segmented cells; SectionHeader white with 2px rule + count chip; header slimmed to [+ New] (primary/inverted) + Settings + Close (Log/Diag removed -- live in Settings > Debugging); all gray tints/separators -> black hairlines. **"This Page" -> "This Note"** (Alex): whole-note scope with a p.N chip per task, sorted by page -- shows where you would jump in a long note. Sources: page link scan + note-wide description refs (page parsed from "<file> p.N") + registry (now covers pending-sync tasks, labeled with a chip). **On Device**: p.N moved into the row chips (outboard column deleted -- alignment fixed); note headers are filesystem-style ("Connor / 1x1") via notePath, which the registry now actually persists (addTask was silently dropping it). **Default tab now includes On Device** (Alex request); also fixed: deep-link focusTab was silently ignored (App.tsx never forwarded it) -- explicit focusTab now wins over config default. ProjectView inherits via shared components; TaskDetail/QuickAdd restyle is a later pass. |
| F-023 | Implemented (awaiting on-device test) | Settings redesign v2 (component vocabulary + apply-on-change) | `design-settings-v2.md` | User feedback: settings page is "all over the place." Root cause: accreted feature-by-feature, four different single-select idioms, ASCII glyph controls that reflow rows on toggle, dishonest grouping (opener gestures under "Handwriting", debugging split across tabs), dead `defaultScreen` control, and a global-Save trap that silently discards changes on Back. Plan (see design doc): five drawn-View primitives (Section/SettingRow/Segmented/Check/InfoSheet -- fixed geometry, no reflow, no RN Switch since it animates and native Supernote toggles are unreachable from the RN tree), one-scroll five-group layout (Account & Sync / Opening SuperTask / Capturing Tasks / Projects / Debugging), apply-on-change with per-row "Saved" chip + persistent header status incl. write-failure state, token keeps explicit Save. Deletes `defaultScreen` (closes T-005 #2). Primitives to be copied to template/ for future plugins once stable. Sequenced AFTER the session-34 gesture/stability test round. |
| F-022 | Phase 1 done (awaiting on-device test) | Holistic logging architecture (capture / transport / discovery) | -- | Three layers: (1) always-on rotating file log; (2) self-healing transport; (3) opt-in remote relay for external-user debugging (would shorten the B-015 loop from days to same-day). **PHASE 1 + runtime URL DONE (session 34 pass 3):** every log entry appends to `MyStyle/SuperTask/logs/session.log` in event-driven batches of 25 (rotates at 512KB to session.log.1; no timers -- they suspend when view closed); `debugServerUrl` is now runtime-configurable (Settings > Connections > Debug Log Server, or USB-edit supertask-config.json) with bundled config.local as fallback only -- fixes T-005 item 6; Upload Log has a 10s abort timeout and always reports where the local copy lives; in-memory buffer 500->2000, Diagnostics motion log 200->1000; dev-server gained GET /ping (identity endpoint for future subnet discovery). **Remaining:** phase 2 subnet sweep + last-known-good candidate racing, phase 3 remote relay. Android can't resolve .local hostnames -- never use them in debugServerUrl. **Setup UX (same session):** Test/ping button (5s timeout, older-server fallback) + Mac/Windows setup popup in Settings; generic `template/dev-server.js` + template README debugging section + CLAUDE.md rewrite so every future plugin inherits the pattern. **Distribution for end users:** dev-server.js is standalone (Node built-ins only, runs from any folder, zero config -- auto-detects IP and prints the URL); attach it next to the .snplg in GitHub releases. Popup instructions are distribution-neutral ("the folder where you saved dev-server.js"). For most external bug reports, session.log over USB suffices -- the server is the live-debugging option. Future nicety: npm-publish as `npx supertask-log-server`. |
| F-021 | Done (awaiting on-device test) | Bezel swipe gesture (reintroduction) | `design-gesture-options.md` | Safely reintroduce bezel swipe as an optional gesture alongside three-finger double tap. Original removal was driven by `_enabled` flag leak (now fixed in session 31), not the gesture concept itself. Approach: wider bezel zone (3-5% vs original 1%), multi-finger touch in bottom edge zone, config-gated (default: off), no direction/displacement complexity. Simpler trigger than original: detect 2+ finger contact in edge zone rather than full swipe-with-direction. Opens task home like three-finger double tap. Could also support configurable targets (today, upcoming, specific project) as the original F-014 did before simplification. **IMPLEMENTED session 34 pass 2:** config-gated (`bezelSwipeEnabled`, default OFF), bottom 4% zone (self-calibrating canvas height, zero SDK calls), >=2 pointers, 150px displacement (80px for 3+), 3500ms max, misreported-DOWN recovery via multi-tap displacement check. Checkbox in Settings > Handwriting. |

## Tasks

| ID | Status | Title | Design doc | Notes |
|----|--------|-------|------------|-------|
| T-001 | Done | Settings page redesign | `design-settings.md` | Compact horizontal layout for e-ink. Connections tab validates API tokens from MyStyle config. Preferences tab uses inline toggles, button rows, and checkbox grids. |
| T-002 | Open | Audit undocumented SDK native modules | -- | `RTNFileModule.java` revealed `openFilePath` dispatches `ACTION_VIEW` with `only_open_file` extra -- not in TS types or docs. Systematically read native Java sources in `android/src/main/java/` for all TurboModules (`RTNFileUtils`, `NativePluginAPI`, `NativePluginManager`, `FileSelector`, `NativeUIUtils`) to find other hidden capabilities. Check Intent extras, undocumented params, internal methods not exposed to JS. |
| T-003 | Open | Write architecture docs, user README, and Ratta feedback | -- | Three deliverables: (1) Architecture doc covering SuperTask's design (temp link nav, gesture detection, task registry, bidirectional linking, offline-first patterns). (2) User-facing README explaining installation, usage, and limitations. (3) Developer feedback for Ratta: API gaps discovered (no goToPage, no openNote, no element fill/background, no writeFile), workarounds used, what would be most helpful (direct note navigation, page switching, element background fill, background task execution). Requires full codebase review to write accurately. |
| T-005 | Open | Session 34 audit -- low-severity follow-ups | -- | Batch of low-priority findings from the session 34 codebase audit: (1) `global.__superTaskButtonId` consumed but never cleared in `App.tsx:56-62` (deep-link global IS cleared; asymmetric) -- stale-route risk on RN context re-creation. (2) `defaultScreen` config saved but never read by `getInitialScreen` -- "Open plugin to" setting has no effect. (3) `Capture.tsx:110` can `nav.resetTo('task-add')` after the user closed the view mid-recognition -- next open shows stale prefilled TaskAdd. (4) `todoist.js` `fetchAllPages` has no max-page ceiling (unbounded loop on a misbehaving cursor). (5) 429 rate-limit responses throw immediately without honoring Retry-After. (6) `debug.js` reads `debugServerUrl` only from bundled config.local at import, never the saved runtime config. **Session 34 pass 2: items 1 (buttonId cleared on consume), 3 (Capture cancelledRef blocks hidden navigate), 4 (MAX_PAGES=20 cap) fixed. Item 2 (defaultScreen) resolved by REMOVAL in the F-023 settings rebuild. Item 6 (debug URL staleness) resolved by F-022 runtime URL. Item 5 (429 Retry-After) remains open.** |
| T-006 | Open | Deprecate/remove three-finger double tap (pending bezel confirmation) | -- | Decision (2026-07-24, with Alex): the tap's "anywhere" design makes it inherently collision-prone with handwriting; bezel swipe is the blessed opener. Tap is dormant (opt-in, default off) NOT deleted yet, for two reasons: (1) the B-028 gates provably cover every observed false fire (round-2 log: false taps at :31-:34 sit inside the 1.5s pen cooldown of pen events at :30 and :35) -- opted-in testing generates free validation data via the new rejection log lines; (2) it is the only fallback opener if the new bezel implementation proves flaky on the external user's device (5-pointer panel, different firmware -- untested). **Removal condition: delete the tap detection code (onMultiTapEnd tap logic, _threeFingerTap, threeFingerTapEnabled config + checkbox) once bezel swipe is confirmed reliable on BOTH the dev A5X and the external user's device.** Multi-tap tracking itself stays -- it swallows multi-touch so the standard gesture path isn't confused. No further engineering on the tap either way. |
| T-004 | Open | SDK call optimization pass | `design-sdk-optimization.md` | Apply `insertTextLink` and bridge-call reduction patterns to remaining hot paths. QuickAdd convert-to-text already optimized (9->4 calls). Remaining locations: **(1) `TaskAdd.tsx` ~line 210-281** -- has the same old 8-call convert pattern (deleteLassoElements -> save -> reload -> insertText -> save -> lasso -> setLassoStrokeLink -> save). Replace with insertTextLink, drop intermediate saves and reloadFile. Saves ~4 calls. **(2) `tempLinkNav.js` ~line 259** -- `reloadFile` after temp link deletion. Plugin view covers the note, reload is invisible. Remove. Saves 1 call. **(3) `ocr.js` ~lines 94-111** -- sequential page context fetches (getCurrentFilePath, getCurrentPageNum, getPageSize). Wrap in `Promise.all`. Saves 2 sequential waits. **(4) `Capture.tsx` ~line 93-109** -- project fetch is sequential with lasso capture. Parallelize with `Promise.all` like QuickAdd does. Saves 1 sequential wait. See design doc for optimization principles. |

## Bugs

| ID | Status | Title | Notes |
|----|--------|-------|-------|
| B-004 | Open | Selected projects in settings isn't honored in the upcoming or today area or projects of supertask | Projects tab fixed: shows all projects (including empty ones), honors `enabledProjectIds` when set. Today/Upcoming tab filtering still open. |
| B-005 | Open | Renaming a note breaks task back-references | Both Todoist description (`Captured from: file.note p.N`) and task registry (`noteFile`) store the bare filename. Renaming the .note file breaks View Note navigation and This Page scan. **Approach:** reconciliation on plugin open -- if a registry noteFile is missing from disk, scan .note files for pages containing a supertask:// link matching the task ID (link elements survive renames). When found, update registry `noteFile` + patch Todoist task description via API. Short-circuit by checking same directory first. |
| B-007 | Done | Long press fires on any empty space (single-link fallback) | `preScanLinks` had a fallback: if only 1 supertask link on page and hit-test fails, use it anyway. This caused ANY long press ANYWHERE on a page with a link to open that task. Fix: removed the fallback entirely. If touch doesn't hit link bounds, long press is ignored. Users access tasks via toolbar/This Page if bounds are unreliable. |
| B-013 | Fixed | recognizeElements error 117 on lower page region (EMR/page size mismatch) | A5X (deviceType=3, fileMachineType=3) reports pageSize 1404x1872 but stroke EMR values reach maxX=20967 maxY=15725 (A5X2 digitizer range, normal max is 15819/11864). Passing reported pageSize to `recognizeElements` causes error 117 for strokes below ~y=1400 pixels. Content in upper page works because EMR values stay within normal range. Confirmed across 3 pages, fresh content, different positions. Native "Convert to Text" works on same content (doesn't use plugin API size param). Fix: detect EMR range from element maxX/maxY and pass A5X2 page size (1920x2560) when EMR values exceed normal range. |
| B-012 | Resolved | Gesture unreliability was `_enabled` flag leak, not phantom pointers | Phantom pointer events after sleep are real (digitizer noise) but were a **red herring** for the primary reliability issue. The actual root cause: `setGestureEnabled(false)` was called when the plugin view opened, but multiple code paths (notably `noteOpener.js` calling `closePluginView()` directly) never called `setGestureEnabled(true)`. This left gestures permanently disabled until the user happened to close via `closePlugin()`. Fix: removed the manual `_enabled` toggle entirely. The SDK's motion listener naturally stops receiving events when the RN plugin view is visible (touch interception). Only `_configOff` (user settings) remains. |
| B-014 | Open | Hardcoded A5X page size (1404x1872) may break Nomad gestures and OCR | Multiple locations fall back to A5X dimensions when `getPageSize()` fails or use A5X EMR constants as detection boundaries. On Nomad (deviceType=4, unknown page dimensions/EMR range), this could cause: (1) incorrect lasso bounds from wrong EMR-to-pixel scale factors, (2) `recognizeElements` error 117 if page size is wrong, (3) temp link positioned incorrectly. **Affected files:** `ocr.js:91` (hardcoded default 1404x1872), `Capture.tsx:46-59` (EMR constants assume only A5X/Manta ranges, scale factor calc at lines 183-184). `tempLinkNav.js` fallback removed (file deleted). **Not affected:** `gestureDetector.js` (uses runtime page height, fail-closed). **To confirm:** need Nomad `getDeviceType()` + `getPageSize()` + element maxX/maxY from device logs. **Fix approach:** call `getDeviceType()` at startup, map to correct dimensions per device, remove hardcoded A5X fallbacks (fail gracefully or use device-specific map). |
| B-015 | Open | `getElements` fails on every pre-scan call for external user | External user logs show every `getElements` call in `preScanLinks` fails, across multiple notes and pages. Long-press-to-view-task is completely broken. Root cause unknown -- previous logging was too sparse. Diagnostic improvement added (session 29): failure now logs error code, message, page number, success value, and result type. Awaiting new logs from external user with improved build. Could be device/firmware specific, note format, or SDK permissions. Same user's touch panel reports up to 5 simultaneous pointers (vs 3 on dev device). Session 34: pre-scan removed (B-021), so `getElements` now fires only on classified gestures -- per-touch failure spam should disappear; whether the rare post-classification scan succeeds on their device still needs testing. |
| B-016 | Resolved | Accidental lasso triggers near bezel zone | Moot: bezel swipe replaced by three-finger double tap (no edge zone). |
| B-017 | Done | TaskHome silent refresh spam (duplicate API calls) | Root cause: `resetTo()` remounts TaskHome with new `key`, each mount called `fetchData()` independently. Fix: `src/cache/taskCache.js` with `_inflightPromise` dedup -- concurrent mounts share one API call. Confirmed on-device: log shows "Joining existing in-flight fetch" (2026-07-06). |
| B-018 | Done | Three-finger double tap feels slow | Fix: gesture prefetch + stale-while-revalidate cache. `fetchTaskData()` fires in gesture handler BEFORE `showPluginView()`, API calls run during React mount. Confirmed on-device: warm opens show "Cache hit" at mount time, ~1s perceived latency (bottleneck is showPluginView SDK overhead). Cold start ~5s (unavoidable: 2s showPluginView + 2s RN mount). |
| B-019 | Fixed (awaiting on-device test) | Gesture detector permanently dies after SDK calls hang | SDK calls in the gesture path can hang indefinitely (plugin view showing, system dialogs). A handler awaiting the hung promise holds `_actionInProgress = true` forever; `onMsg`'s `if (_actionInProgress) return;` then drops ALL future events. **Session 33 fix (`withTimeout`, 5s) was insufficient on its own**: JS timers are suspended while the plugin view is closed (documented on-device learning), which is exactly when gestures run -- the timeout may never fire. **Session 34 fix: event-driven watchdog in `onMsg`** -- if `_actionInProgress` has been held > 8s, the next motion event force-clears it (the motion stream is the one thing guaranteed to keep flowing while the view is closed). An action token (`_actionId` via `beginAction()`/`endAction()`) prevents a hung handler's late `finally` from clobbering a newer action's guard. Worst case is now one dropped gesture, never a dead session. `withTimeout` retained as a first line of defense for when timers do run. |
| B-020 | Fixed (awaiting on-device test) | Native element leak when `getElements` times out in gesture scan | When `withTimeout(getElements(...))` timed out, the underlying promise kept running; if it later resolved, the returned elements were never `recycle()`d -- each timeout leaked a page's worth of native-side element memory. On a device with intermittent SDK hangs this compounds over a session and may have contributed to device degradation. Fix (session 34): on timeout, attach a continuation to the original promise that recycles late-arriving elements. |
| B-028 | Fixed (awaiting on-device retest) | Palm + pen writing falsely triggers task-home gestures (bezel recovery + phantom three-finger taps) | **Found in on-device testing 2026-07-24** (log 18-22-13): palm/hand-edge contacts DO reach the motion listener during pen writing (contradicts the earlier "palm rejection is firmware-perfect" finding -- single-device, no-writing test). Palm cluster enters multi-tap tracking; with two contacts far apart, MOVE y-coords jump between contact points, producing phantom "1059px upward displacement" during normal writing -- the bezel-swipe recovery path (session 34) fired `openTaskHome` mid-writing, and false `Three-finger tap (first)` recordings appeared. **Fix:** (1) pen events now CANCEL bezel tracking and POISON multi-tap tracking (`penSeen`) -- a poisoned cluster is never interpreted as tap or swipe; poison carries in from `_mixedInput` when multi-touch starts mid-pen; real taps/swipes never involve pen contact. (2) The displacement-based bezel recovery is REMOVED entirely -- its trigger is indistinguishable from palm coordinate-jumping; the primary bezel path (DOWN in edge zone, confirmed working same log: 3 fingers, 539px, 343ms) is the only trigger. Residual risk closed same day with two more gates on the primary bezel path: **spatial gate** (additional PTR_DOWNs must land in the bottom 40% band -- a contact far above is palm+finger, tracking cancelled; band is generous because fingers register staggered 50-150ms) and **continuity filter** (only MOVE steps <= 120px count toward displacement; larger single-event jumps are the stream switching contact points, exactly the phantom-displacement mechanism). Pointer count alone was evaluated and rejected as the discriminator: palm clusters legitimately register 2-3 "fingers" (see the log), phantoms inflate counts, and panels disagree on caps (3 vs 5). Count stays as the existing >=2 gate only. **Round 2 (same day, log 18-50-44):** palm re-plants BETWEEN strokes are pen-FREE clusters -- poisoning missed them, and two palm plants within 800ms fired a false three-finger double tap mid-page (log shows pen at :30, 'taps' at :31-:34, pen at :35 -- a writing rhythm). Fix: `_lastPenTime` pen cooldown -- no tap or swipe may fire within 1500ms of ANY pen event (`PEN_COOLDOWN_MS`), and a tap cluster must last <= 600ms (`TAP_MAX_MS`; a resting palm isn't crisp). UX consequence, documented: after writing, pause ~1.5s before three-finger tapping or bezel swiping. |
| B-022 | Fixed (awaiting on-device test) | No fetch timeout + `_inflightPromise` poisoning permanently kills task loading | **Critical.** `todoistFetch` (`todoist.js:43`) has no timeout/abort. `taskCache.js` stores the pending promise in `_inflightPromise` and clears it only in `finally` -- if a fetch hangs (wifi drop mid-request), the promise never settles, `_inflightPromise` never clears, and every future `fetchTaskData()` joins the same dead promise for the life of the process (days). TaskHome stuck on "Loading tasks..." forever; gesture prefetch shares the poisoned promise. Also: retry backoff uses `setTimeout` (`todoist.js:40`), which is suspended if the view closes mid-backoff, stalling the chain. Fix: AbortController timeout on all fetches + clear `_inflightPromise` via a race-with-deadline (not just `finally`). Found in session 34 audit. **FIXED session 34 pass 2:** `fetchWithTimeout` (AbortController, 20s) in todoist.js with retry on timeout/network error; `taskCache` abandons in-flight fetches older than 90s via identity-checked slot (timer-free backstop). |
| B-023 | Fixed (awaiting on-device test) | Lasso elements never recycled in QuickAdd/Capture/ocr.js -- native memory leak on every capture | **Critical for device stability.** `QuickAdd.tsx:134` and `Capture.tsx:129` call `getLassoElements()` and never call `.recycle()` on any path (success or error); `ocr.js` doesn't recycle the array it receives either. Every lasso capture (toolbar button 200 or gesture) leaks a selection's worth of native element memory, degrading the plugin host over a long session. (TaskHome and gestureDetector recycle correctly -- these are the only offenders.) Found in session 34 audit. **FIXED session 34 pass 2:** shared `recycleElements()` in ocr.js; QuickAdd and Capture recycle in `finally` on every path. |
| B-024 | Fixed (awaiting on-device test) | Untimed SDK awaits freeze screens while holding guard flags (duplicate-task risk) | **High.** (1) `TaskAdd.handleSubmit`: `setLassoStrokeLink`/`saveCurrentNote` awaited with no timeout AFTER `createTask` succeeds and before `setSubmitting(false)` -- a hang leaves the button stuck on "Adding…" and a retry creates a duplicate Todoist task. (2) `QuickAdd.handleConvertToText`/`handleDone`: hang while `marking=true` disables BOTH Done and X (X early-returns on `marking`) -- overlay un-dismissable except tap-outside. (3) `TaskDetail` save/complete/delete: `saving` stuck on hang; in deep-link mode the success path never resets `saving` (`nav.pop()` is a no-op with no back stack). Fix: `withTimeout` wrapper (as in ocr.js) + always reset guards in `finally` + timeout-aware status text. Found in session 34 audit. **FIXED session 34 pass 2:** `withTimeout` on all SDK awaits in TaskAdd/QuickAdd handlers (5-8s); TaskAdd resets `submitting` in `finally`; TaskDetail complete/delete route through `leaveAfterMutation` (pop with stack, closePlugin in deep-link mode). Network-side hangs bounded by B-022 fix. |
| B-025 | Fixed (awaiting on-device test) | Non-atomic RNFS writes + read-modify-write races can corrupt/lose persisted state | **Medium.** `config.js:236` and `taskRegistry.js:49` write JSON directly (no temp-file+rename): a process kill mid-write leaves truncated JSON; registry's parse-failure fallback then wipes the whole local task index (`emptyRegistry()`). Concurrent `addTask` calls when `_cache` is null each read-modify-write independently -- last writer wins, a task registry entry is silently lost. Fix: write-to-temp+rename, serialize writes through a promise chain. Found in session 34 audit. **FIXED session 34 pass 2:** temp-file+rename writes with crash recovery from temp; mutation serialization chain in taskRegistry; load dedup + save chain in config.js. |
| B-026 | Fixed (awaiting on-device test) | Obfuscation `btoa` throw silently discards the entire saved config | **Medium.** `xorEncode` (`config.js:76`) can produce char codes > 255 for non-ASCII input; Hermes `btoa` then throws. The throw is swallowed by `loadFromFile`'s outer try, which returns null -- plugin boots with defaults and an EMPTY API token, silently disabling sync. Trigger: any non-ASCII byte (e.g. smart-quote) pasted into the USB-edited config. Fix: try/catch around obfuscation per-field (keep plain value on failure) + validate/strip non-ASCII on load. Found in session 34 audit. **FIXED session 34 pass 2:** per-field try/catch keeps plain value on encode failure; obfuscation rewrite isolated so it can never abort the load. |
| B-027 | Fixed (awaiting on-device test) | Diagnostics motion listener has no unmount cleanup | **Medium.** `Diagnostics.tsx:33,72-109`: listener registered into a module var, removed only by the explicit Stop button. Navigating away without Stop leaves it running for the session -- alongside the gesture detector's listener -- calling `setState` on the unmounted component on every touch event. Fix: `useEffect` cleanup that removes the sub on unmount. Found in session 34 audit. **FIXED session 34 pass 2:** unmount cleanup stops the listener (log survives in module state); UI updates routed via module-level `_uiNotify` hook so a live listener never touches a dead component. Also fixed missing `useEffect` import. |
| B-021 | Fixed (awaiting on-device test) | Pre-scan on every finger DOWN suspected of device-level interference | `onFingerDown` fired 3 SDK calls (`getCurrentFilePath`, `getCurrentPageNum`, `getElements`) over the AIDL bridge on EVERY finger touch, including during active writing -- the leading hypothesis for the "device feels unreliable" conflicts that got the plugin uninstalled, and a likely aggravator of B-015 (external user's per-touch getElements failures). Fix (session 34): architectural refactor -- `onMsg` is now SDK-free (pure JS coordinate/timing tracking); the link scan (`scanLinksAt`) runs only AFTER a gesture is classified on finger UP (long press or lasso-add gate). Normal taps, scrolls, and pen strokes now cost zero bridge calls. Trade-off: long press gains a few hundred ms before the view opens (imperceptible next to showPluginView's ~2s). |

